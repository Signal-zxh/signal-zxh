run:
  timeout: 5m

linters:
  disable-all: true
  enable:
    - errcheck
    - gofmt
    - goimports
    - govet
    - ineffassign
    - staticcheck
    - unused

linters-settings:
  errcheck:
    check-type-assertions: true
    check-blank: false

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
    - path: main\.go
      linters:
        - errcheck
    - path: service/cache/post\.go
      linters:
        - errcheck
    - path: service/post\.go
      linters:
        - errcheck